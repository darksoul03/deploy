---
- name: dev
  hosts: all

 
  tasks:
 
  - name: install the apache2 package
    shell: sudo apt-get -y install apache2
  - name: Copy file with owner and permissions
    copy:
      src: /var/lib/jenkins/workspace/browser/dev/
      dest: /var/www/html/
